interface:
  display_name: "Session Done"
  short_description: "Prepare a handoff; retain Markdown only when requested."
  icon_small: "./assets/icon.jpg"
  icon_large: "./assets/icon.jpg"
  brand_color: "#111827"
  default_prompt: "Prepare a concise session handoff with verified outcomes, blockers, and next actions. Return it in chat unless I request a saved note."

policy:
  allow_implicit_invocation: true
